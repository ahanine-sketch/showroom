import React from 'react';
import BehaviorScorecard from '@/components/scorecard/BehaviorScorecard';

export default function Page() {
  return <BehaviorScorecard role="owner" activeTab="behavior" />;
}
